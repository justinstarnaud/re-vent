export interface ProductPreview {
    handle: string;
    title: string;
    image: string;
    price: string;
    address: string; // hardcode
    city: string; // hardcode
    province: string; // hardcode
    category: string; // hardcode
}